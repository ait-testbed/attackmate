vars:
  METASPLOITABLE: 192.168.0.146
  ATTACKER: 192.168.0.30
commands:
  - type: remote
    cmd: execute_command
    connection: primary_node
    remote_command:
      type: shell
      cmd: hostname
    metadata:
      description: "Execute the shellcommand 'hostname' on a remote attackmate instance"
