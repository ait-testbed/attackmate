###

vars:
  $SERVER_ADDRESS: localhost

commands:
  - type: shell
    cmd: echo hello world

  - type: msf
    cmd: echo whoot

  - type: shell
    cmd: nmap $SERVER_ADDRESS
    error_if: .*test.*
    error_if_not: ipp
    loop_if_not: ippa
